<%= form_for @invite do |f| %>
    <%= f.hidden_field :event_id, value: @event_id %>
    Пользователи:<%= f.collection_select :user_id, @users, :id, :name %><br>
    <%= f.submit "Invite" %>
<% end %>